-- Phase 04 governed landing location for immutable source files.

CREATE VOLUME IF NOT EXISTS workspace.devin_market_risk_dev.bronze_landing
COMMENT 'Governed landing area for immutable Bronze source files and manifests';
